#!/bin/zsh

if [ -f /etc/profile ] ; then 
	source /etc/profile
fi

# variables
export LANG='en_US.UTF-8'

PATH=${PATH}:~/.local/bin
PATH=${PATH}:~/.cargo/bin
PATH=${PATH}:~/.gem/ruby/2.7.0/bin:/usr/lib/ruby/gems/2.7.0/
PATH=${PATH}:~/.cabal/bin
# TODO fix this and make it better (check if ruby is installed lol)
# PATH=${PATH}:~/.gem/ruby/$(ruby -v | grep -Po '\d+\.\d+\.\d+')/bin
export PATH


export EDITOR='kak'

export GITHUBMAIL='33614480+em-dash@users.noreply.github.com'


# this is from coreutils
# idk if there is a sensible way to make this always work
# TODO
# eval $(dircolors)


